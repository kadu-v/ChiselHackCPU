class Main {
   function void main() {
        var int c;
        var int d;
        let c = Uart.receiveChar();
        let d = Uart.receiveChar();
        
        
        if ((c = 2) & (d = 108)) {
            do Memory.poke(8201, 1);
        }
        return;
   }
}
