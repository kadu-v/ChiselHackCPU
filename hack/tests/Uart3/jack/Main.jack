class Main {
   function void main() {
        var int c;
        var int reg;
        let c = Uart.receiveChar();
        if (c = 10) {
            let c = Uart.receiveChar();
            if (c = 20) {
               let reg = Memory.peek(8192);
               do Memory.poke(1024, 2);
               return;
            }
            do Memory.poke(1024, c);
            return;
        }

        do Memory.poke(1024, c);
        return;
   }
}
